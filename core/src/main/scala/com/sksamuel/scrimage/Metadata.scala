package com.sksamuel.scrimage

import javax.imageio.metadata.IIOMetadata

/** Created by guw on 03/09/14.
  */

class Metadata(iio: IIOMetadata) {
  //  def childrenIterator(format: String): Iterator[Node] = new Iterator[Node]{
  //    var current = iio.getAsTree(format)
  //    def hasNext: Boolean =
  //
  //    def next(): Node =
  //  }
  //
  //  def iterateOnAttributes(node: Node): Unit ={
  //
  //  }
}
